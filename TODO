std::endl instead of '\n' ?

A partir de cpp02: canonical form:
- Default constructor
- Copy constructor
- Destructor
- Copy assignment operator

class A final
{
public:
	A();
	A(const A &a);
	~A();
	A &operator=(const A &a);
};
